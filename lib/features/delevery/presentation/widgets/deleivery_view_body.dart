import 'package:flutter/material.dart';

class DelieveryViewBody extends StatelessWidget {
  const DelieveryViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('DelieveryViewBody'));
  }
}
